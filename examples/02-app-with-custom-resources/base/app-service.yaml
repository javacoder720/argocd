apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  labels:
    app: web-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: web-app
