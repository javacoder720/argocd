apiVersion: data.example.com/v1
kind: Database
metadata:
  name: app-db
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  engine: postgres
  version: "15.3"
  storageSize: "10Gi"
  backup:
    enabled: false
    schedule: "0 2 * * *"
