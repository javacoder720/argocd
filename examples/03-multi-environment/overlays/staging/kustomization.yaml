apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

commonLabels:
  environment: staging

patches:
  - path: database-patch.yaml
  - path: deployment-patch.yaml
